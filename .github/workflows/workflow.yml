name: Deploy Spring Boot to VPS

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: |
          set -e
          mvn clean package -DskipTests


      - name: Backup current JAR on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          timeout: 60s
          script: |
            cd /root/dvente-api
            
            if [ -f app.jar ]; then
              echo "💾 Sauvegarde de l'ancienne version..."
              TIMESTAMP=$(date +%Y%m%d_%H%M%S)
              cp app.jar "app.jar.backup_$TIMESTAMP"
            
              # Garder seulement les 5 dernières sauvegardes
              ls -t app.jar.backup_* 2>/dev/null | tail -n +6 | xargs -r rm
            
              echo "✅ Sauvegarde créée: app.jar.backup_$TIMESTAMP"
            else
              echo "ℹ️ Aucune version précédente à sauvegarder (premier déploiement)"
            fi

      - name: Copy JAR to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          source: "target/*.jar"
          target: "/root/dvente-api"
          strip_components: 1
          timeout: 300s

      - name: Deploy on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          timeout: 300s
          script: |
            set -e
            cd /root/dvente-api
            systemctl restart springboot-app
            sleep 15
            
      - name: Deployment successful
        if: success()
        run: echo "🎉 Déploiement réussi sur le VPS!"
